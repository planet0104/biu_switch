# 蓝牙音箱固件设计文档

## 项目概述

基于 **nRF52840 + Rust (embassy-nrf)** 的蓝牙音箱固件，通过 **JDY-68A 蓝牙音频模块** 接收手机蓝牙音频，经 **PAM8406 数字功放** 驱动喇叭播放。支持自动关机、按钮唤醒、播放状态检测等功能。

---

## 一、硬件模块清单

| 序号 | 模块 | 型号/规格 | 数量 | 功能说明 |
|------|------|-----------|------|----------|
| 1 | 主控开发板 | Promicro NRF52840（兼容 nice!nano V2.0） | 1 | 运行 Rust 固件，管理电源、通信、状态检测 |
| 2 | 蓝牙音频模块 | JDY-68A（单声道输出） | 1 | 蓝牙 5.1 音频接收 + BLE 双模，输出模拟音频 |
| 3 | 固态继电器 | XYF-1H6（Ron=0.25Ω, IL<1μA, 负载 0.8A/60V） | 1 | 控制 PAM8406 功放电源通断 |
| 4 | 数字功放板 | PAM8406（带音量电位器，立体声 5W×2） | 1 | 功率放大，驱动喇叭 |
| 5 | 喇叭 | 4Ω / 3W（单只） | 1 | 音频输出 |
| 6 | 锂电池 | 3.7V 18650（带保护板） | 1 | 整机供电 |
| 7 | 限流电阻 | 330Ω | 1 | 固态继电器 IN+ 限流保护 |
| 8 | 按钮 | 常开型轻触开关 | 1 | 唤醒/开机 |

---

## 二、开发板引脚资源分配

Promicro NRF52840 开发板可用排针及功能分配：

| 开发板丝印 | 芯片引脚 | 功能 | 连接对象 |
|-----------|---------|------|---------|
| VCC | — | 3.3V 可控输出（P0.13 内部控制） | JDY-68A VCC(1) |
| RAW | — | 电池直通 3.0~4.2V | XYF-1H6 OUT+ |
| 3.3V | — | 常开 3.3V（不受 P0.13 控制） | 未使用 |
| BAT+ / BAT- | — | 锂电池充放电管理 | 锂电池 |
| D0 | P0.08 | GPIO 输出 | XYF-1H6 IN+（经 330Ω 限流电阻） |
| D1 | P0.06 | UART RX | JDY-68A TXD(11) |
| D2 | P0.17 | GPIO 输入（内部上拉） | JDY-68A STAT(6) |
| D10 | P0.09 | GPIO 输入（内部上拉） | 按钮 → GND |
| D19 | P0.02 | UART TX | JDY-68A RXD(12) |
| P0.13 | — | 内部 GPIO（未引出排针） | 控制开发板 VCC 引脚通断 |

> **注**：
> - nRF52840 的 UART 引脚可通过 PSEL 寄存器任意映射，P0.02/P0.06 组合完全支持 UART 通信。
> - **P0.09 / P0.10 默认是 NFC 天线脚**。固件必须启用 embassy-nrf 的 `nfc-pins-as-gpio`，才能把 D10（P0.09）当作普通 GPIO 使用。

---

## 三、接线总表

### 3.1 电源供电线

| 起点 | 终点 | 线色 | 说明 |
|------|------|------|------|
| 锂电池 **+** | 开发板 **BAT+** | 红 | 充放电管理输入 |
| 锂电池 **-** | 开发板 **BAT-** | 黑 | 系统共地 |
| 开发板 **VCC** | JDY-68A **VCC(1)** | 橙 | **3.3V，P0.13 内部控制通断** |
| 开发板 **RAW** | XYF-1H6 **OUT+** | 红 | 电池直通 3.8V |
| XYF-1H6 **OUT-** | PAM8406 **VCC** | 红 | OUT 无极性，可互换 |
| 开发板 **GND** | JDY-68A **GND(16)** | 黑 | 共地 |
| 开发板 **GND** | PAM8406 **GND** | 黑 | 共地 |

### 3.2 继电器控制线

| 起点 | 终点 | 线色 | 说明 |
|------|------|------|------|
| 开发板 **D0 / P0.08** | **330Ω 电阻** 一端 | 黄 | GPIO 控制功放电源 |
| **330Ω 电阻** 另一端 | XYF-1H6 **IN+** | 黄 | 限流保护，驱动光耦 LED |
| XYF-1H6 **IN-** | **GND** | 黑 | 控制回路 |

> **控制逻辑**：P0.08 = High → 继电器导通 → 功放上电；P0.08 = Low → 继电器关断 → 功放断电（漏电流 < 1μA）

### 3.3 UART 通信线

| 起点 | 终点 | 线色 | 说明 |
|------|------|------|------|
| 开发板 **D1 / P0.06** (RX) | JDY-68A **TXD(11)** | 蓝 | nRF 接收 ← JDY 发送 |
| 开发板 **D19 / P0.02** (TX) | JDY-68A **RXD(12)** | 绿 | nRF 发送 → JDY 接收 |

### 3.4 状态检测线

| 起点 | 终点 | 线色 | 说明 |
|------|------|------|------|
| 开发板 **D2 / P0.17** | JDY-68A **STAT(6)** | 紫 | 蓝牙连接状态，高=已连，低=断开 |

### 3.5 音频信号线（摘要）

| 起点 | 终点 | 线色 | 说明 |
|------|------|------|------|
| JDY-68A **AUDIO(10)** | PAM8406 **LIN（左声道输入）** | 白 | 单声道模拟音频 |
| JDY-68A **GND(16)** | PAM8406 **AGND / GND（音频地）** | 黑 | 信号参考地，必须接 |

> 详见下方 **§3.8～3.11** 功放板与喇叭完整说明。RIN 悬空。

### 3.6 喇叭输出线（摘要）

| 起点 | 终点 | 线色 | 说明 |
|------|------|------|------|
| PAM8406 **LOUT+ / L+** | 喇叭 **+** | 红 | 左声道 BTL 正极 |
| PAM8406 **LOUT- / L-** | 喇叭 **-** | 黑 | 左声道 BTL 负极 |

> ROUT+/ROUT- 不接喇叭。详见 **§3.8～3.11**。

### 3.7 唤醒按钮线

| 起点 | 终点 | 线色 | 说明 |
|------|------|------|------|
| 开发板 **D10 / P0.09** | 按钮 **一端** | 灰 | 内部上拉输入 |
| 按钮 **另一端** | **GND** | 黑 | 按下时 P0.09 被拉低，触发中断唤醒 |

### 3.8 PAM8406 功放板端子说明

市售「PAM8406 带电位器」小板丝印不完全统一，按下表对照板子上的焊盘/排针（名称可能略有差异）：

| 板端丝印（常见） | 本项目接法 | 说明 |
|------------------|------------|------|
| **VCC / VIN / +** | ← XYF-1H6 **OUT-**（或 OUT+，OUT 无极性） | 功放电源正极，来自开发板 **RAW**（电池直通约 3.0～4.2V） |
| **GND / PGND / −** | ← 开发板 **GND**（与电池负极共地） | 功率地；与音频地在板内通常已连通 |
| **LIN / L-IN / L** | ← JDY-68A **AUDIO(10)** | 左声道音频输入 |
| **RIN / R-IN / R** | **不接（悬空）** | JDY-68A 为单声道输出，右输入闲置 |
| **AGND / GND（输入侧）** | ← JDY-68A **GND(16)** | 音频参考地；若板上输入侧只有一个 GND，与电源 GND 共焊亦可，但建议与 JDY 地短线直连 |
| **LOUT+ / L+ / LS+** | → 喇叭 **+** | 左声道桥式输出正极（BTL） |
| **LOUT- / L- / LS-** | → 喇叭 **−** | 左声道桥式输出负极（BTL） |
| **ROUT+ / R+ / RS+** | **不接** | 右声道输出闲置 |
| **ROUT- / R- / RS-** | **不接** | 右声道输出闲置 |
| **音量电位器** | 板上自带，可调 | 建议先旋到约中间再上电试听 |
| **MUTE / SHDN**（若有） | 一般悬空或按板厂默认 | 本方案用继电器整板断电，可不接这两脚 |

**电气要点：**

1. **PAM8406 为 BTL（桥式）输出**：喇叭两端都接在 `LOUT+` / `LOUT-` 上，**不要**把喇叭一端接到系统地 GND。
2. **供电必须走 RAW → 继电器 → 功放 VCC**，不要从开发板 3.3V / VCC（LDO）取电；功放峰值电流远超 LDO 能力。
3. **单声道只用左通道**：输入进 LIN，喇叭接 LOUT±；右通道输入/输出全部空置，避免误接第二只喇叭导致电流翻倍。
4. 若希望略增响度，可将 **AUDIO 同时并到 LIN 与 RIN**，喇叭仍只接 **LOUT±**（右输出仍空置）——可选，不是本项目默认接法；默认仅接 LIN，更简单、功耗略低。

### 3.9 喇叭选型与接线

| 项目 | 要求 | 说明 |
|------|------|------|
| 阻抗 | **4Ω**（推荐） | 与 PAM8406 常见模块匹配；不要用低于 4Ω 的负载 |
| 额定功率 | **≥ 3W**（本清单 4Ω/3W） | 电池约 3.7V 时单声道实际功率通常小于标称 5W，3W 喇叭一般够用 |
| 极性 | 喇叭 **+** → LOUT+，**−** → LOUT− | 接反只会相位反相，能响但与多喇叭系统相位不一致；单喇叭可接受 |
| 线径 | 建议 ≥ 22 AWG（约 0.3mm²）短线 | 喇叭线尽量短，远离天线/UART 线 |
| 数量 | **1 只** | 接在左声道；勿并联两只 4Ω（等效 2Ω，易过热保护/失真） |

**禁止事项：**

- 禁止将 `LOUT+` 或 `LOUT-` 对 GND 短路试音。
- 禁止在功放已上电时反复对插喇叭端子（易打火、爆音）。
- 禁止把右声道 ROUT± 与左声道 LOUT± 并联去推同一只喇叭。

### 3.10 音频通路完整接线图

```
手机 ──A2DP──→ JDY-68A
                 │
                 │ AUDIO(10) ──────────────→ PAM8406 LIN
                 │ GND(16)  ──────────────→ PAM8406 AGND/GND
                 │
                 │              ┌── LOUT+ ──→ 喇叭 +
                 │              │
开发板 RAW ──→ XYF-1H6 ──→ PAM8406 VCC
                 │              │
开发板 GND ───────────────→ PAM8406 GND
                 │              │
                 │              └── LOUT- ──→ 喇叭 −
                 │
                 │   RIN / ROUT+ / ROUT- ：全部悬空
                 │
开发板 D0 ──330Ω──→ XYF-1H6 IN+     （功放电源开关）
GND ──────────────→ XYF-1H6 IN-
```

**推荐装配顺序（首次接线）：**

1. 先接共地：电池 −、开发板 GND、JDY GND、功放 GND。
2. 再接功放电源：RAW → 继电器 OUT → 功放 VCC（此时勿给继电器 IN 高电平）。
3. 接音频：JDY AUDIO→LIN，JDY GND→功放 AGND。
4. 接喇叭：LOUT+ / LOUT−（确认极性）。
5. 电位器旋到约 50%。
6. 最后上电固件，确认开机时序为「先 JDY、后功放」后再试听。

### 3.11 功放 / 喇叭接线检查清单

接线或排故时按表逐项确认：

| # | 检查项 | 期望 |
|---|--------|------|
| 1 | 功放 VCC 来自 RAW+继电器，而非 3.3V/VCC | 是 |
| 2 | 功放 GND 与开发板 / 电池负极共地 | 是 |
| 3 | JDY AUDIO → 功放 **LIN** | 是 |
| 4 | JDY GND → 功放输入地 | 是 |
| 5 | 功放 **RIN** 悬空（默认方案） | 是 |
| 6 | 喇叭接 **LOUT+ / LOUT−**，未接系统 GND | 是 |
| 7 | **ROUT±** 空置 | 是 |
| 8 | 喇叭为 4Ω，且仅一只（未并联） | 是 |
| 9 | 继电器 IN+ 经 **330Ω** 到 D0，IN− 接 GND | 是 |
| 10 | 首次试听前电位器约中间位 | 是 |
| 11 | 关机时功放可被继电器完全断电 | 是（否则待机数 mA） |

**常见现象对照：**

| 现象 | 可能原因 |
|------|----------|
| 完全无声 | 功放未上电（继电器未导通）；AUDIO/LIN 接错；喇叭接在 ROUT；音量电位器在最小 |
| 只有沙沙声/噪声大 | 音频地未接；地线过长成环；AUDIO 线与电源线绞在一起 |
| 开机「砰」一声 | 功放早于蓝牙上电，或关机顺序反了（见 §4.2） |
| 大声破音 | 电位器过大 + 手机音量过大；喇叭功率不足 |
| 一放歌就重启/掉电 | 功放误接在 3.3V LDO；电池内阻过大或保护板限流 |
| 待机耗电高 | 继电器未能关断，PAM8406 静态约数 mA |

---

## 四、电源架构与时序

### 4.1 电源分配策略

```
锂电池 3.7V
    │
    ├──→ 开发板 BAT+/BAT-（充放电管理 + LDO）
    │       │
    │       ├──→ P0.13 控制开关 ──→ VCC 引脚(3.3V) ──→ JDY-68A
    │       │
    │       └──→ RAW 引脚(3.8V) ──→ XYF-1H6 ──→ PAM8406
    │
    └──→ 所有 GND 单点共地
```

| 模块 | 供电来源 | 控制方式 | 电压 |
|------|---------|---------|------|
| nRF52840 | 内部 LDO | 始终通电 | 3.3V |
| JDY-68A | VCC 引脚 | P0.13 内部控制 | 3.3V |
| PAM8406 | RAW 引脚 | P0.08 + XYF-1H6 继电器 | 3.0~4.2V |

### 4.2 上电/关机时序（防爆音 + 防 GPIO 倒灌）

**开机时序（按钮唤醒后）**：
1. P0.13 = High → VCC 引脚通电 → **JDY-68A 上电**（3.3V）
2. 延时 600ms → 等待 JDY-68A 蓝牙初始化完成
3. 恢复 UART/STAT 引脚为正常工作模式
4. P0.08 = High → 继电器导通 → **功放上电**（RAW 3.8V）
5. 蓝牙已稳定 → 功放后开 → **无爆音**

**关机时序（10分钟无活动后）**：
1. **P0.02(TX)** 拉低 → 防止电流通过 JDY-68A RX 保护二极管倒灌
2. P0.08 = Low → 继电器关断 → **功放先断电**
3. 延时 50ms → 确保功放完全断电
4. P0.13 = Low → VCC 引脚断电 → **JDY-68A 后断电**
5. nRF52840 进入 **SYSTEM OFF**，仅保留 P0.09 按钮 SENSE 唤醒（唤醒后为冷启动）

> **原则**：开机先蓝牙后功放，关机先功放后蓝牙。防止初始化噪声或断电噪声被喇叭放大。
>
> **说明**：相对“System ON idle”，SYSTEM OFF 更易做到 µA 级待机；代价是唤醒不保留 RAM，需完整重新上电初始化（对本音箱可接受）。

---

## 五、Rust 固件功能设计

### 5.1 项目架构（模块划分）

```
src/
├── main.rs              # 初始化 + 任务调度 + 电源命令循环
├── power.rs             # 电源管理：P0.13 + P0.08 时序，SYSTEM OFF
├── jdy68a.rs            # JDY-68A：UART 事件解析 + STAT 监控（含 AT 发送辅助）
├── audio_state.rs       # 音频状态机：连接/断开/播放/暂停
├── auto_shutdown.rs     # 自动关机：10 分钟定时器 + 活动检测
├── button.rs            # 运行中按键消抖（取消倒计时）；SYSTEM OFF 唤醒在 power.rs
├── events.rs            # 事件通道（AudioEvent / PowerCommand / ShutdownCtrl）
├── board_led.rs         # 板载 LED（保留，后续可改状态指示）
├── battery.rs           # 电池采样（保留）
├── usb_log.rs           # USB CDC defmt + BOOT 命令（保留）
├── bootloader.rs        # UF2 软入口（保留）
└── pins.rs              # 引脚与时间常量
```

### 5.2 核心模块功能说明

#### 5.2.1 Power Manager（电源管理）

- **输入**：PowerCommand 通道（PowerOff；开机由冷启动后 `main` 直接调用）
- **输出**：控制 P0.13（JDY-68A 电源）和 P0.08（功放继电器）
- **职责**：
  - 封装 GPIO 输出控制逻辑
  - 实现严格的上电/关机时序（含延时）
  - 关机前将 **P0.02(TX)** 拉低，防止 GPIO 倒灌电流到已断电的 JDY-68A
  - 提供 `is_powered_on()` 状态查询
  - 关机完成后进入 SYSTEM OFF，P0.09 SENSE 唤醒

#### 5.2.2 JDY-68A Driver（蓝牙模块驱动）

- **UART 配置**：9600 波特率，无流控，P0.02(TX) / P0.06(RX)
- **AT 指令接口**：
  - `AT+PLAY` / `AT+PAUSE` / `AT+NEXT` / `AT+LAST` — 播放控制
  - `AT+VOLUP` / `AT+VOLDN` / `AT+VOLUME=x` — 音量控制（0~16）
  - `AT+STAT` / `AT+PSTAT` — 查询连接/播放状态
  - `AT+DISC` — 主动断开蓝牙
- **事件解析器**：异步 UART 接收任务，解析 JDY-68A 主动上报的事件：
  - `+CONNECTING<<MAC` / `+CONNECTED` → 音频蓝牙已连接
  - `+DISC:SUCCESS` → 蓝牙已断开
  - `+PLAY=xxx` → 开始播放（含设备名）
  - `+PAUSE=` → 暂停/停止播放
  - 每 1.5s 轮询 `AT+STAT`（JDY-68A 连接=`1,0` / 断开=`0,0`）
  - **播放状态**：本模块 `AT+PSTAT` 无回复；若固件开启 ENLOG，可能主动上报 `+PLAY=` / `+PAUSE=`，否则无法用串口区分播放/暂停（自动关机退化为「断开后计时」）
- **STAT 引脚监控**：P0.17；注意双模模块上 STAT 常为 **BLE** 状态，手机仅连音频时可能一直为低，不能单靠该脚判断音箱连接

#### 5.2.3 Audio State Machine（音频状态机）

```
状态定义：
    Disconnected ──[+CONNECTED]──→ ConnectedIdle
    ConnectedIdle ──[+PLAY]──────→ Playing
    Playing ────────[+PAUSE]────→ Paused
    Paused ─────────[+PLAY]─────→ Playing
    ConnectedIdle ──[+DISC]─────→ Disconnected
    Playing ────────[+DISC]─────→ Disconnected
    Paused ─────────[+DISC]─────→ Disconnected
```

- **输入**：AudioEvent 通道（Connected / Disconnected / PlaybackStarted / PlaybackPaused）
- **输出**：状态变更通知（用于 LED 指示、日志、自动关机逻辑）
- **职责**：维护当前音频状态，触发状态转换回调

#### 5.2.4 Auto Shutdown（自动关机）

- **触发条件**：
  - 蓝牙断开（STAT 变低 / `+DISC:SUCCESS`）→ 启动 10 分钟倒计时
  - 播放暂停（`+PAUSE=`）→ 启动 10 分钟倒计时
  - 蓝牙连接（`+CONNECTED`）或开始播放（`+PLAY=`）→ 取消倒计时
- **定时器**：使用 `embassy_time::Timer`，支持中途取消
- **超时动作**：
  1. 发送 PowerOff 命令
  2. 执行关机序列
  3. nRF52840 进入 SYSTEM OFF
- **策略降级**：若 JDY-68A 不主动上报 `+PAUSE=` 事件，可退化为“蓝牙断开 10 分钟后关机”的保守策略（STAT / `+DISC` 仍可用）

#### 5.2.5 Button Task（按钮）

- **引脚**：P0.09（D10），内部上拉输入（需 `nfc-pins-as-gpio`）
- **运行中**：下降沿 + 50ms 软件消抖 → 取消自动关机倒计时（视为活动）
- **待机唤醒**：SYSTEM OFF 下由 P0.09 SENSE=LOW 唤醒，冷启动后自动执行开机时序

#### 5.2.6 Low Power Management（低功耗管理）

- **正常工作模式**：所有外设运行，CPU 全速
- **关机待机模式**：
  - JDY-68A 断电（P0.13 = Low）
  - PAM8406 断电（P0.08 = Low）
  - UART 外设关闭，TX 保持低电平
  - CPU 进入 **SYSTEM OFF**，仅保留 P0.09 按钮 SENSE
  - 待机电流目标 < 10μA（需拔掉 USB，仅电池供电测量）

---

## 六、关键设计约束与注意事项

| 序号 | 约束 | 说明 |
|------|------|------|
| 1 | **JDY-68A 必须接 VCC 引脚，不能接 3.3V 引脚** | VCC 受 P0.13 控制可关断；3.3V 是常开 |
| 2 | **P0.08 与继电器 IN+ 之间必须串 330Ω 电阻** | 防止光耦 LED 过流烧毁 GPIO |
| 3 | **关机前必须将 P0.02(TX) 拉低** | 防止电流通过 JDY-68A RX 保护二极管倒灌到 VCC |
| 4 | **PAM8406 必须从 RAW 引脚取电** | RAW = 电池直通，电流充足；LDO 带不动功放峰值电流 |
| 5 | **所有模块 GND 单点共地到电池负极** | 防止地环路噪声 |
| 6 | **上电先蓝牙后功放，关机先功放后蓝牙** | 防止爆音和断电噪声 |
| 7 | **JDY-68A 启动时间约 600ms** | 上电后需延时再发 AT 指令 |
| 8 | **PAM8406 音量电位器先调至中间位置** | 再用手机蓝牙音量做精细调节，避免削波失真 |
| 9 | **充电跳线 BOOST 仅在电池 > 500mAh 时短接** | 否则保持默认 100mA 充电电流 |
| 10 | **PAM8406 静态电流约 3.5mA** | 必须断电才能实现真正零待机 |
| 11 | **P0.09 需 `nfc-pins-as-gpio`** | 否则 D10 不能当 GPIO，按钮无法使用 |
| 12 | **喇叭必须接 LOUT±（BTL），禁止一端接 GND** | 见 §3.8～3.9 |
| 13 | **仅使用左声道推一只 4Ω 喇叭** | RIN/ROUT 默认悬空；勿并联两只 4Ω |
---

## 七、系统框图

```
┌─────────────────────────────────────────────────────────────────────┐
│                           用户交互层                                 │
│  ┌─────────┐                                                        │
│  │  手机   │  ←── 蓝牙 A2DP ──→  JDY-68A 蓝牙音频模块               │
│  │ (音源)  │                        │                                │
│  └─────────┘                        │ AUDIO(10) 单声道模拟音频        │
│                                     ↓                                │
│                              PAM8406 功放板                          │
│                                     │                                │
│                                     ↓                                │
│                                  4Ω 喇叭                             │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                           控制管理层                                 │
│                                                                      │
│  Promicro NRF52840 开发板                                            │
│  ┌──────────────────────────────────────────────────────────────┐     │
│  │  Rust 固件 (embassy-nrf)                                     │     │
│  │                                                              │     │
│  │  ┌─────────────┐  ┌─────────────┐  ┌──────────────────┐   │     │
│  │  │ Power       │  │ Auto        │  │ Audio State      │   │     │
│  │  │ Manager     │  │ Shutdown    │  │ Machine          │   │     │
│  │  │             │  │ (10min)     │  │                  │   │     │
│  │  │ P0.13 ──VCC │  │             │  │ Disconnected     │   │     │
│  │  │ P0.08 ──Relay│ │             │  │ ConnectedIdle    │   │     │
│  │  └─────────────┘  └─────────────┘  │ Playing          │   │     │
│  │         ↑                  ↑        │ Paused           │   │     │
│  │         └──────────────────┘        └──────────────────┘   │     │
│  │                                                              │     │
│  │  ┌─────────────┐  ┌─────────────┐  ┌──────────────────┐   │     │
│  │  │ JDY-68A     │  │ Button      │  │ Low Power        │   │     │
│  │  │ Driver      │  │ Task        │  │ Manager          │   │     │
│  │  │ (UART/AT)   │  │ P0.09 ──Btn │  │ SYSTEM OFF       │   │     │
│  │  └─────────────┘  └─────────────┘  └──────────────────┘   │     │
│  └──────────────────────────────────────────────────────────────┘     │
│                                                                      │
│  外设连接：                                                          │
│  • P0.02 ──TX──→ JDY-68A RXD(12)                                    │
│  • P0.06 ←─RX──── JDY-68A TXD(11)                                    │
│  • P0.17 ←─STAT── JDY-68A STAT(6)                                    │
│  • P0.13 ──VCC──→ JDY-68A VCC(1) [内部控制]                         │
│  • P0.08 ──Relay→ XYF-1H6 ──→ PAM8406 VCC                           │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                           电源架构                                   │
│                                                                      │
│  锂电池 3.7V                                                         │
│     │                                                                │
│     ├──→ BAT+/BAT- → 开发板充放电管理 → LDO 3.3V → VCC 引脚         │
│     │                                      │                        │
│     │                                      ├──→ P0.13 开关 ──→ JDY  │
│     │                                      │                        │
│     │                                      └──→ nRF52840 自身供电   │
│     │                                                                │
│     └──→ RAW 引脚 ──→ XYF-1H6 继电器 ──→ PAM8406 VCC               │
│                        ↑                                             │
│                        └── P0.08 控制                                │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 八、开发环境

| 项目 | 版本/说明 |
|------|-----------|
| 语言 | Rust |
| 异步运行时 | embassy |
| HAL | embassy-nrf |
| 芯片 | nRF52840 |
| 构建目标 | thumbv7em-none-eabi（软浮点；与本仓库 `.cargo/config.toml` 一致） |
| 烧录工具 | UF2（`flash.bat`）/ probe-rs / J-Link / DAP-Link |
| 特殊 feature | `nfc-pins-as-gpio`（P0.09 按钮） |

---

*文档版本: v1.2*  
*最后更新: 2026-08-13*

### 修订记录

| 版本 | 修正 |
|------|------|
| v1.2 | 补充 **PAM8406 端子对照、喇叭选型、音频通路图、装配顺序、检查清单与排故表**（§3.8～3.11） |
| v1.1 | 关机拉低引脚由错误的 P0.05 改为实际 UART TX **P0.02** |
| v1.1 | 待机由 System ON idle 改为 **SYSTEM OFF**（与实现一致，利于 µA 待机） |
| v1.1 | 构建目标由 `eabihf` 改为 **`thumbv7em-none-eabi`** |
| v1.1 | 补充 P0.09 NFC 复用 GPIO 的 `nfc-pins-as-gpio` 约束 |
| v1.1 | 模块文件名与仓库实现对齐（`power.rs` 等），保留 LED/USB log/电池模块 |
